#HIGH Priority
you must make sure you understand the JigSawDotNet pattern and use. Intention is to reduce and branching code by genertaing code whihc may require implmenting or re-using Ops, Kernel and Factory patterns,
Review Sharmind.Core.Activations.* for example

Tasks to implement.
Task 1: Named interfaces.
These interfaces will need a public string Name { get; init; } for later use.
IContextCompactor, IPromptPostProcessor, IPromptPreProcessor, IGenerator

Task 2: Native ChatSession Compactor(s)
Native IContextCompactor(s) for Compacting Session
Summarize version: use working model as Agent to Summaraize and mark previous non IsPinned ChatMessages as Ignore and so prompt formatter is not using entire History.
Truncate version: Simply truncate remove previously non IsPinned History by making oldest is Ignore.

Task 3: CUI Plugins.
Read Folder Plugins to add to OptionsView addition interfaces that can be loaded and used.
Will need to avoid Name Conflicts, first in wins.
IContextCompactor, IPromptPostProcessor, IPromptPreProcessor, IGenerator(may require PluginGeneratorBuilder implmenting IGeneratorBuilder)

Task 4: CUI ChatMessage Names.
OptionsView should have an option to set User Name but default to standard User.
ChatSession, ChatSessionBridge and DebugChatBridge when adding messages to history should use the correct name and if it is from the Model use the Agent Name set buy the AgentBuilder.

Task 5: Optimization
5.1: Regex cehck for regex usages example Gpt2PreTokeniser, Cl100kPreTokeniser create a private static readonly Regex Pattern but should be implementing a [GeneratedRegex] see SharpMind.Inference.RegexGenerated.
There are lot of added System.Text.RegularExpressions.Regex.* calls JinjaTemplateFormatter that have slowed perfomance but can be patially redeemed by calling generated regexes in RegexGenerated.
5.2: Check for creations of objjects that really need to only be created once in construction or outside of loops.
5.3: Look at call sites that throw exceptions or tests bounds or conditions where in release these check should no longer be nessarcay, if they are compleatly redundant they could be moved but if still worth keeping in a Debug enviroment these check can be moved into an new class SainityChecks calling static methods maked as [Conditional("DEBUG")].  Methods should be re-uasable as possible.

Task 6: Config and Factory Consistancy.
Check if Config and Mapping overrides are correct by means of simplicity and end user user.
Check facorties are consistant in use and implementations.

Task 7: Model Loading, Full and Streaming.
Currenly we operate in a "Full" mode loading all layers on initialization.



Task 8: Loading time.
new ChatSession can take a while,  if possible is it possible to an IProgress to the constructor or add an public void InitializeChat(IProgress<float>? progress = null); that can be used by CUI to show after initialization of weight, unclear where exatly all the time is consumend.

Task 9: SharpMind Native Model
Design ModelFormat exclusivly for SharpMind.  This format should include all relevent metadata and layers with QuantDType layer positions and lenght for loading.
Additional it should include Plugins such as classes with [ToolDesc] that can be included by default and or custom plugs.  these can be stored as byte arrays and loaded. CUI should be able to hable adding these to OptionsView.
Then we need the final End to End Test, Super Dumb Model Training save and Load, suggest training on small data set.

Task 10: Future Incorperate Training into CUI.