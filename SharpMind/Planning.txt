#HIGH Priority
you must make sure you understand the JigSawDotNet pattern and use. Intention is to reduce and branching code by genertaing code whihc may require implmenting or re-using Ops, Kernel and Factory patterns,
Review Sharmind.Core.Activations.* for example

Tasks to implement.

CUI add last session to File Menu if it exists and in ModelBrowserView add button for the last loaded model if it exists. Use model name as button txt.


Task 10: SharpMind Native Model
Design ModelFormat exclusivly for SharpMind.  This format should include all relevent metadata and layers with QuantDType layer positions and lenght for loading.
Additional it should include Plugins such as classes with [ToolDesc] that can be included by default and or custom plugs.  these can be stored as byte arrays and loaded. CUI should be able to handle adding these to OptionsView and if possible excluding [ToolDesc] mark as (recomended) as text beside option.
Then we need the final End to End Test, Super Dumb Model Training save and Load, suggest training on small data set.

Task 11: Future Incorperate Training into CUI.

Task 12: Cleanup, commenting,  unused private methods, naming.

Profiler for all VecDot quants and its variants to ensure correct implementation.  SSE faster than Scalar, AVX2 faster than SSE, FMA faster than AXV.
And a profiler to compare Serial Vs Parallel MatMul on syntestic data approximatly size of 1b model layer

gemma models
[formatter ERROR] JINJA ERROR: variable '(message' not found in template env
[formatter ERROR] JINJA ERROR: variable ''user') != (loop.index0' not found in template env
[formatter ERROR] JINJA ERROR: variable '0)' not found in template env

Testing Qwen2-0.5B.Q6_K
[formatter] prompt=<|im_start|>system\nYou are a helpful assistant

I think testing on a smaller model is fine, if qwen2-0_5b-instruct-q8_0 has 24 layers and were only loading 2 ant initialization layer 0 and the next layer 1 asynchronously memory should be around 85-90% less so instead of 6000MB resident in memory only 900MB.