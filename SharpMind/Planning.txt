Apps:
	Cli: entry point, commands, config
	Benchmarks: performance, accuracy, throughput
	Samples: usage examples, notebooks
Runtime:
	Inference: sampling, kv-cache, generation loop
	Training: optimizer, scheduler, checkpointing

Model: transformer, attention, FFN, norms
Tokenizer: BPE, vocab builder, encode/decode
Data: sources, cleaning, formating, batching, dataset loaders
Core: tensors, matrix math, memeory, activations

in this topic "With real training data and more steps" PseudoLanguageGenerator was intended to be a clss to produce an imaginary language with syntax and human like composition so could that be improved to be used to as a test of performance.
It may not be read world but concept cand be ammended designed to be a proof of correctness/working.
or bring in and alphabet from Kaggel to test for clissification. What real training data would you reccoment and 
should we add Read Safetensors and GGUF for model weights now or can this be later, training a larger model may be an issue on this device but if smaller ones suffice for now thats fine.

Next Actions
Inference/decoding - Add generate/predict methods for autoregressive sampling  
Inference project started will need work

Evaluation - Add perplexity, accuracy metrics
Checkpointing - Save/load model weights
New feature - Something else you're interested in?




Config files hould be human readable
Skills.MD
Read Safetensors and GGUF for model weights
IDatasource additions
Read Parquet
Read CSV 
should PseudoLanguageSerializer have SaveTo txt or csv

Use of JsonObject, preffer stronly typed class where possible.

Would like to introduce 

LLM lobotomy
Take a model and strip it of all but necessity for becoming an expert wich could be re-merge for MOE type LLM  

LLM size refinement
Quantization: This reduces the precision of model weights (e.g., from 16-bit floating point to 8-bit or 4-bit integers), significantly decreasing memory requirements and improving inference speed
Knowledge Distillation: A smaller "student" model is trained to mimic the behavior of a larger "teacher" model, allowing the smaller model to achieve higher accuracy than if it were trained from scratch
Pruning: Less important weights are removed from the neural network, resulting in a lighter model. 
LoRA (Low-Rank Adaptation): A fine-tuning technique that keeps base model weights frozen while training a smaller number of parameters, making fine-tuning more resource-efficient

LLM weight merging
Weighted Averaging/Linear Merge: Takes a weighted average of the parameters from multiple models
SLERP (Spherical Linear Interpolation): Better than linear interpolation for high-dimensional spaces, as it preserves the magnitude of weights, reducing the loss of performance often seen in simple averagin
TIES-Merging (TrIm, Elect, Sign, and Merge): Designed to handle interference between models by only merging the most significant weight updates and resolving sign disagreements
DARE (Drop And REscale): Drops redundant weights and re-scales the remaining weights to create a "soups" of models, reducing task interference.
Task Arithmetic: Creates "task vectors" by subtracting the base model weights from the fine-tuned model weights, allowing these vectors to be added or subtracted to modify model behavior.