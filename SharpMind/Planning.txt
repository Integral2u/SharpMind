Apps:
	Cli: entry point, commands, config
	Benchmarks: performance, accuracy, throughput
	Samples: usage examples, notebooks
Runtime:
	Inference: sampling, kv-cache, generation loop
	Training: optimizer, scheduler, checkpointing

Model: transformer, attention, FFN, norms
Tokenizer: BPE, vocab builder, encode/decode
Data: sources, cleaning, formating, batching, dataset loaders
Core: tensors, matrix math, memory, activations


In ProductionTraining var sharpConfig = SharpMindConfig.Gpt with { Hardware = HardwareTier.Gpu }; fails as HardwareTier has no Gpu value.
MappingBuilder needs extension methods from SharpMind.GPU and existing confinguration systems unified into SharpMind.
Also PipelineNode had no Static From method and should IDataSource extend  IAsyncEnumerable<string>

TokenizationPipeline
MUST DO

BpeTrainer needs massive optimization, See RealDataTraining line 76.

Ensure these are done, Remeber implementing the JigSawDotNet pattern may be applicable for options
Model Compression, Quantization (4-bit/8-bit/2-bit/1-bit/1.58-bit), Distillation, Pruning/Sparsity
Inference Speed Optimizations, Flash Attention, Speculative Decoding is Fused Kernels pratical, TurboQuant.
Memory & Context Optimizations, Key-Value (KV) Caching, PagedAttention, Grouped-Query Attention (GQA

Can we use the EMNIST dataset to test builing of a model and testing?
The EMNIST dataset is a set of handwritten character digits derived from the NIST Special Database 19  and converted to a 28x28 pixel image format and dataset structure that directly matches the MNIST dataset
I have the MatLab or Gzip files downloaded that i can dd to ExternalAssests

Please cite the following paper when using or referencing the dataset:
Cohen, G., Afshar, S., Tapson, J., & van Schaik, A. (2017). EMNIST: an extension of MNIST to handwritten letters. Retrieved from http://arxiv.org/abs/1702.05373

Unify Configs in SharpMind.Core and flesh our MappingBuilder

ChatSession design (async), tool, input types, status(Thinking/Updating/Executing/Responding/Waiting)

Next Actions
Inference/decoding - Add generate/predict methods for autoregressive sampling  
Inference project started will need work

Evaluation - Add perplexity, accuracy metrics
Checkpointing - Save/load model weights
New feature - Something else you're interested in?




Config files hould be human readable
Skills.MD
Read Safetensors and GGUF for model weights
IDatasource additions
Read Parquet
Read CSV 
should PseudoLanguageSerializer have SaveTo txt or csv

Use of JsonObject, preffer stronly typed class where possible.

Would like to introduce 

LLM lobotomy
Take a model and strip it of all but necessity for becoming an expert wich could be re-merge for MOE type LLM  

LLM size refinement
Quantization: This reduces the precision of model weights (e.g., from 16-bit floating point to 8-bit or 4-bit integers), significantly decreasing memory requirements and improving inference speed
Knowledge Distillation: A smaller "student" model is trained to mimic the behavior of a larger "teacher" model, allowing the smaller model to achieve higher accuracy than if it were trained from scratch
Pruning: Less important weights are removed from the neural network, resulting in a lighter model. 
LoRA (Low-Rank Adaptation): A fine-tuning technique that keeps base model weights frozen while training a smaller number of parameters, making fine-tuning more resource-efficient

LLM weight merging
Weighted Averaging/Linear Merge: Takes a weighted average of the parameters from multiple models
SLERP (Spherical Linear Interpolation): Better than linear interpolation for high-dimensional spaces, as it preserves the magnitude of weights, reducing the loss of performance often seen in simple averagin
TIES-Merging (TrIm, Elect, Sign, and Merge): Designed to handle interference between models by only merging the most significant weight updates and resolving sign disagreements
DARE (Drop And REscale): Drops redundant weights and re-scales the remaining weights to create a "soups" of models, reducing task interference.
Task Arithmetic: Creates "task vectors" by subtracting the base model weights from the fine-tuned model weights, allowing these vectors to be added or subtracted to modify model behavior.


The last session seems to be broken can we continue here.  this is what we were up to Phase 2: Add Real Data Converters
A. open-perfectblend (Parquet)

Contains ~1.5GB of training data (6 shards)
Unknown schema - need to inspect
B. fusechat (JSON)

Already have a sample
Format: {conversations: [{from, value}, ...]}
Need ParquetSource class to read .parquet files
Actions:

Add Parquet package reference to read files
Create ParquetSource class implementing IDataSource
Create FusechatSource for JSON conversations
Create new training sample sets (not replace FullTraining) and you had created the project SharpMind.Data.Parquet but i believe it was broken so i have removed it, re-added an new projects and added the Parquet.Net package


can SchemaInspector be removed
Phase 3: Use Pseudo-Language for Optimal Size
Goal: Determine model size guidance from learnable patterns

Actions:

Run learnable with varying:
vocab: small (16) → medium (64) → large (128)
hidden: 16 → 32 → 64 → 128
layers: 1 → 2 → 3
Track loss curves to find where returns diminish
Use this to size model for real data training


Phase 4: EMNIST Digit Recognition
Goal: Test building/training a vision model

Data:

You have MatLab or GZIP files (not in ExternalAssets yet)
28x28 grayscale images
62 classes (digits 0-9, uppercase A-Z, lowercase a-z)
Actions:

Add EMNIST files to ExternalAssets
Create EMNISTLoader to parse
Create simple CNN or MLP model
Train and evaluate accuracy
Questions for Clarification
Capacity increase: Should I proceed with vocab=64, hidden=64, layers=1 (start small) or go bigger?
Data converters priority: Which should come first?
Parquet (larger dataset, for language model pre-training)
Fusechat (Q&A format, closer to learnable structure)
EMNIST files: Do you have these ready to add to ExternalAssets? What format - MatLab (.mat) or GZIP (.gz)?
FullTraining changes: Should new data samples be:
Separate sample sets (e.g., FullTrainingRealData.cs)
Added as new syntax patterns to LearnableGenerator?
